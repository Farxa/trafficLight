import { styled } from "@mui/material/styles";
import { Button } from "@mui/material";

export const PedestrianButton = styled(Button)(() => ({
  height: "fit-content",
  width: "fit-content",
  position: "absolute",
  top: 16,
  left: 56,
}));
